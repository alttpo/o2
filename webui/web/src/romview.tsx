import {ROMViewModel, ViewModel} from "./viewmodel";
import {CommandHandler, TopLevelProps} from "./index";
import {Component, Fragment} from 'preact';

export default ({ch, vm}: TopLevelProps) => {
    return (
        <Fragment></Fragment>
    );
};
